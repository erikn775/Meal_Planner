<h1>Welcome <%=@user.name.capitalize %></h1>

<h2>Meal Plans:</h2>
<h3>Week of:</h3>
<%= is_empty?(@user.meal_plans) %>
<ul>
<% @user.meal_plans.each do |plan| %>
    <li><%= link_to(plan.week_of, meal_plan_path(plan)) %> - <%= link_to("Shopping List", shopping_list_path(plan)) %></li>
<% end %>
</ul>

<h2>Shopping List</h2>


<h2>Your Recipes</h2>
<%= render 'recipe' %>


